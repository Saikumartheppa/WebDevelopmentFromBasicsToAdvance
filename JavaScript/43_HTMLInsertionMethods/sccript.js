// let ele = document.getElementsByTagName('div')[0]
// // ele.innerHTML = ele.innerHTML +"<h1> hello Wolrd </h1>"

// let div = document.createElement('div');
// div.innerHTML = "<h1> hello Wolrd </h1>"
// ele.appendChild(div);

let first = document.getElementsByClassName('container')[0];
let div = document.createElement('div');
div.innerHTML = "<h1> hello Wolrd </h1>"
// ele.appendChild(div);

// first.prepend(div); // creats element inside the container and adds at first
// first.append(div); // add the element at last

// first.before(div);
// first.after(div); // creates a element outside the conatiner next to it

// first.replaceWith(div);

// first.replaceChildren(div);